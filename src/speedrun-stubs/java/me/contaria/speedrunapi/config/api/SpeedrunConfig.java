package me.contaria.speedrunapi.config.api;

/**
 * Minimal compile-time stub of SpeedrunConfig used so the mod can compile when
 * the real Speedrun API is not present in the dev environment. This file is
 * intentionally minimal and should be removed when compiling against the real API.
 */
public interface SpeedrunConfig {
    String modID();
}
